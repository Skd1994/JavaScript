var s = "occurance is happening";
		for(var i=0; i<s.length; i++)
		{
			var ch = s.charAt(i);
			if(ch=='a'||ch=='e'||ch=='i'||ch=='o'||ch=='u')
			{
				console.log(ch+" ");   //o u a e i a e i 
            }
        }